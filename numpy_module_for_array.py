#if a package or module is not present in-built so we can install as below:
#Go to command promt enter (pip) and if something is written then go for the write
#(pip install package_name) Ex.pip install numpy
import numpy as nm


nm_array=nm.array([1,2,5,4,7])

print(nm_array)
print(nm_array[1])
